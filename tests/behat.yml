default:
  calls:
    error_reporting: 7 # value of E_ERROR | E_WARNING | E_PARSE
  formatters:
    progress: true
  suites:
    default:
      contexts:
        - FeatureContext
        - Drupal\DrupalExtension\Context\MinkContext
        - Drupal\DrupalExtension\Context\DrupalContext
        - Drupal\DrupalExtension\Context\RawDrupalContext
        - Drupal\EntityReferenceGenerator\Context\EntityReferenceGeneratorContext
      filters:
         tags: '~@wip'
  extensions:
    Behat\MinkExtension:
      base-url: 'http://127.0.0.1:8008'
      files_path: '%paths.base%/features/files/'
      goutte: ~
      javascript_session: 'selenium2'
      selenium2: ~
    Drupal\DrupalExtension:
      api_driver: 'drupal'
      text:
        password_field: "edit-pass"
        username_field: "edit-name"
      drupal:
        drupal_root: '/vagrant/repos/patches/drupal_demo/web/core'
      blackbox: ~
      drush: ~
      drush:
        root: '/vagrant/repos/patches/drupal_demo/web/core'
      subcontexts:
        paths:
          - "/vagrant/repos/patches/drupal_demo/tests"
        autoload: 0
      region_map:
        content: ".region-content"
        header: ".region-header"
        footer: ".region-footer"
      selectors:
        message_selector: '.messages'
        error_message_selector: '.messages.messages--error'
        success_message_selector: '.messages.messages--status'
  formatters:
    pretty: true
